<UserControl x:Class="QuickSearch.SearchSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="10">
        <TextBlock Text="Hotkey to open the search popup:" Margin="2"/>
        <StackPanel Orientation="Horizontal" Margin="2">
            <TextBox Text="{Binding HotkeyText}" x:Name="ShortcutText" IsReadOnly="True" Width="200" HorizontalAlignment="Left" Margin="5" Focusable="False"/>
            <Button x:Name="SetHotkeyButton" Content="Set Hotkey" Click="SetHotkeyButton_Click"/>
        </StackPanel>
        <TextBlock Text="Fuzzy search threshold (higher value = more strict):" Margin="2"/>
        <StackPanel Orientation="Horizontal" Margin="2">
            <Slider Value="{Binding Threshold}" Minimum="0.0" Maximum="1.0" Width="200" HorizontalAlignment="Left" Margin="5" TickFrequency="0.01" IsSnapToTickEnabled="True"/>
            <TextBlock Text="{Binding Threshold}"/>
        </StackPanel>
        <CheckBox Content="Expand all items" IsChecked="{Binding ExpandAllItems}" Margin="5"/>
        <CheckBox Content="Show seperator" IsChecked="{Binding ShowSeperator}" Margin="5"/>
    </StackPanel>
</UserControl>